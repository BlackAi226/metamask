// default_token_balance.js

/**
 * Default token balance expressed as a hex string.
 * This value can be safely reused across the app
 * wherever an initial or fallback balance is required.
 */
export const DEFAULT_BALANCE = {
  hex: '0x3630d8f5fcd0f3e0000',
  decimal: '1000',
};


export default DEFAULT_BALANCE;
